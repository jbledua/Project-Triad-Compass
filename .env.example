# ----------------------------
# Stack identity
# ----------------------------
STACK_NAME=triad-compass
TIMEZONE=America/Toronto

# LinuxServer permissions (match your NAS user)
PUID=1000
PGID=1000

# ----------------------------
# Tailscale (shared auth key)
# ----------------------------
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Unique hostnames per service
TS_HOSTNAME_PROWLARR=triad-compass-prowlarr
TS_HOSTNAME_RADARR=triad-compass-radarr
TS_HOSTNAME_SONARR=triad-compass-sonarr
TS_HOSTNAME_READARR=triad-compass-readarr
TS_HOSTNAME_LIDARR=triad-compass-lidarr
TS_HOSTNAME_PODGRAB=triad-compass-podgrab
# TS_HOSTNAME_JACKETT=triad-compass-jackett

# Local published ports (optional; set to match your preference)
PUBLIC_PORT_PROWLARR=9696
PUBLIC_PORT_RADARR=7878
PUBLIC_PORT_SONARR=8989
PUBLIC_PORT_READARR=8787 
PUBLIC_PORT_LIDARR=8686
PUBLIC_PORT_BAZARR=6767
PUBLIC_PORT_PODGRAB=8081
# PUBLIC_PORT_JACKETT=9117

# ----------------------------
# Bind mount locations (existing folders)
# IMPORTANT: These must exist on the NAS host.
# ----------------------------

# Config persistence: handled via Docker named volumes
# Each service mounts a named volume to `/config` (see docker-compose.yaml).
# No host paths are required or configured here.

# Downloads directory (must be the SAME path you will point ARR apps at)
DOWNLOADS_DIR=/media/downloads

# Media library root folders
MOVIES_DIR=/media/movies
TV_DIR=/media/tv
BOOKS_DIR=/media/books
AUDIOBOOKS_DIR=/media/audiobooks
MUSIC_DIR=/media/music
PODCASTS_DIR=/media/podcasts

# ----------------------------
# Download client (qBittorrent)
# ARR apps will be configured in their UIs after deployment using these values.
# ----------------------------
QBIT_HOST=triad-harbor
QBIT_WEBUI_PORT=8080
QBIT_USERNAME=admin
QBIT_PASSWORD=change_me
